# 🎨 Темы оформления для Dictovod

## Что это?

Темы позволяют настроить внешний вид программы под ваши предпочтения: цвета, шрифты, размеры элементов и даже символы кнопок.

## Как использовать темы

### 1. Выбор существующей темы

1. Запустите Dictovod
2. Нажмите кнопку меню `☰`
3. Выберите **Темы** → нужная тема
4. Тема применится мгновенно

### 2. Создание своей темы

1. Откройте папку `themes` рядом с программой
2. Скопируйте файл `default.json`
3. Переименуйте копию (например, `mytheme.json`)
4. Откройте файл в текстовом редакторе
5. Измените параметры (см. ниже)
6. Сохраните файл
7. Перезапустите программу или выберите тему в меню

## Параметры темы

### Основные настройки

```json
{
  "Name": "Моя тема",           // Название темы
  "Version": "1.0"              // Версия (для совместимости)
}
```

### Цвета (в формате HEX)

```json
{
  "BackgroundColor": "#d3d7cf",   // Фон окна
  "ButtonColor": "#8fa8b3",       // Кнопки (меню)
  "SuccessColor": "#a9cba4",      // Кнопка Play (зелёный)
  "DangerColor": "#cc6666",       // Кнопка Stop/Close (красный)
  "TextColor": "#000000",         // Цвет текста
  "StatusTextColor": "#00008B"    // Цвет статуса внизу
}
```

💡 **Совет**: Используйте онлайн-палитры цветов (например, colorhunt.co) для подбора гармоничных сочетаний.

### Шрифты

```json
{
  "FontFamily": "Courier",        // Название шрифта
  "FontSize": 8.0                 // Размер шрифта
}
```

**Популярные шрифты**:
- `Courier`, `Courier New` — моноширинные
- `Arial`, `Segoe UI` — современные
- `Consolas` — для программистов
- `Comic Sans MS` — весёлый стиль

### Размеры окна

```json
{
  "WindowWidth": 160,             // Ширина окна
  "WindowHeight": 65,             // Высота окна
  "ButtonSize": 20,               // Размер кнопок
  "TopPanelHeight": 20            // Высота верхней панели
}
```

### Символы кнопок

```json
{
  "PlaySymbol": "▶",              // Кнопка запуска
  "PauseSymbol": "⏸",             // Кнопка паузы
  "MenuSymbol": "☰",              // Кнопка меню
  "CloseSymbol": "✖"              // Кнопка закрытия
}
```

**Альтернативные символы**:
- Play: `►`, `▷`, `▸`, `>`
- Pause: `⏸`, `❚❚`, `||`, `⏯`
- Menu: `☰`, `≡`, `⋮`, `⚙`
- Close: `✖`, `×`, `✕`, `X`

## Готовые темы

### 🌟 Default (по умолчанию)
Классическая светлая тема с мягкими цветами

### 🌙 Dark
Темная тема для работы в ночное время

### ⚪ Minimal
Минималистичная светлая тема

### 💾 Retro
Ностальгическая DOS-тема в стиле 90-х

### 🔍 Large
Увеличенные элементы для комфортного использования

## Примеры модификаций

### Сделать окно больше

```json
{
  "WindowWidth": 200,
  "WindowHeight": 80,
  "ButtonSize": 25,
  "FontSize": 10.0
}
```

### Неоновая киберпанк-тема

```json
{
  "BackgroundColor": "#0a0a0a",
  "ButtonColor": "#1a1a2e",
  "SuccessColor": "#00ff41",
  "DangerColor": "#ff006e",
  "TextColor": "#00ff41",
  "StatusTextColor": "#00d4ff"
}
```

### Пастельная мягкая тема

```json
{
  "BackgroundColor": "#faf3f0",
  "ButtonColor": "#d4b5b0",
  "SuccessColor": "#b8e0d2",
  "DangerColor": "#eac4d5",
  "TextColor": "#5a5a5a",
  "StatusTextColor": "#6a8caf"
}
```

## Поделиться своей темой

Создали крутую тему? Отправьте JSON-файл разработчику или поделитесь с другими пользователями!

## Устранение проблем

**Тема не применяется**
- Проверьте правильность JSON-синтаксиса
- Убедитесь, что файл в папке `themes`
- Перезапустите программу

**Странные цвета**
- Цвета должны быть в формате `#RRGGBB`
- Используйте только шестнадцатеричные символы (0-9, A-F)

**Не отображается текст**
- Проверьте контраст между `TextColor` и `BackgroundColor`
- Используйте светлый текст на темном фоне или наоборот

---

**Приятной кастомизации!** 🎨✨
